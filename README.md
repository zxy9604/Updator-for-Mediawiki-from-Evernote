# About

这玩意儿属于即兴写出的小东西 `mediawiki.py`完成了连接Mediawiki的API并且完成一些工作(如login, edittext)的功能, `Evernote.py`基本上是根据Evernote的API给的例程修改的...其实没有什么意思 仅仅是读取Evernote的文件而已.

很简单的代码, 也并不是很费功夫, 所以很快就弄完了

Mediawiki的错误判断还没弄, 因为有点赶时间. 抽空加一个对Error的处理就好了.

